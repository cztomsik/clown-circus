// Shared Tailwind class tokens (referenced by more than one component).
// Every utility references var(--color-*), so flipping <html data-theme>
// recolours them automatically. Tokens used by a single primitive live with
// that primitive in primitives.tsx.
export const PRE = 'm-0 py-2 px-2.5 whitespace-pre-wrap break-words';
